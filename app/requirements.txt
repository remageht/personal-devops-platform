fastapi==0.115.0
uvicorn[standard]==0.30.6
prometheus-client==0.21.0
docker==7.1.0
python-dotenv==1.0.1
# NOTE: убраны неиспользуемые passlib / python-jose / itsdangerous / jinja2 / multipart.
# Верните их только когда реально внедрите JWT-auth + формы, иначе это лишний attack surface.
